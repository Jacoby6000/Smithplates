<%@ val ctx: com.jacoby6000.smithy.stache.sql.codegen.ServiceTemplateView %>
from ${serviceModuleBaseName(ctx.serviceName)}_models import (
<% for (model <- ctx.models) { %>
    ${model.name},
<% } %>
<% for (union <- ctx.unions) { %>
    ${union.name},
<% } %>
)

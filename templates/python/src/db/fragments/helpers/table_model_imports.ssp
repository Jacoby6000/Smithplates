<%@ val ctx: com.jacoby6000.smithplates.sql.service.renderer.ServiceTemplateView %>
from ${qualifiedModule(ctx.packageName, modelsModulePath(ctx.serviceModuleName))} import (
<% for (model <- ctx.models) { %>
    ${model.name},
<% } %>
<% for (union <- ctx.unions) { %>
    ${union.name},
<% } %>
)

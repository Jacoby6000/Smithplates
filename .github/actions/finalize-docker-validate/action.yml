name: Finalize Docker validate
description: Export the Docker test image so Actions can persist it in cache.

runs:
  using: composite
  steps:
    - name: Export Docker test image
      shell: bash
      run: ./scripts/ci-docker-export-image.sh

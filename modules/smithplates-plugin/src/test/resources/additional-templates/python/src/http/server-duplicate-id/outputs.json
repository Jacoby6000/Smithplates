{
  "shared": [
    {
      "id": "custom.http.dup",
      "artifactKind": "src",
      "template": "custom_once.ssp",
      "outputPath": "{{smithyNamespaceDir}}/one.py",
      "binding": { "type": "once" }
    },
    {
      "id": "custom.http.dup",
      "artifactKind": "src",
      "template": "custom_once.ssp",
      "outputPath": "{{smithyNamespaceDir}}/two.py",
      "binding": { "type": "once" }
    }
  ]
}

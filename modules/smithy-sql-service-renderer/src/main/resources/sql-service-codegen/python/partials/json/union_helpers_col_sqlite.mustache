def _read_{{className}}_col(row: sqlite3.Row, column: str) -> {{className}}:
{{> partials/json/row_data_sqlite_col}}{{newline}}    present = [key for key in ({{{discriminatorKeys}}}) if key in data]
    if len(present) != 1:
        raise ValueError(f"unknown {{className}} discriminator: {sorted(data.keys())}")
    return cast({{className}}, data)

def _read_str(row: ${rowTypeName(attrs("dialectKey").asInstanceOf[String])}, index: int) -> str:
    value = row[index]
    if isinstance(value, uuid.UUID):
        return str(value)
    return cast(str, value)

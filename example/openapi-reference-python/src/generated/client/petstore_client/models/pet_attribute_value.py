# coding: utf-8

"""
    Petstore

    HTTP-facing petstore service for smithplates FastAPI codegen and Smithy OpenAPI export.

    The version of the OpenAPI document: 2024-01-01
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from petstore_client.models.color import Color
from petstore_client.models.vaccinated import Vaccinated
from petstore_client.models.weight_kg import WeightKg
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

PETATTRIBUTEVALUE_ONE_OF_SCHEMAS = ["Color", "Vaccinated", "WeightKg"]

class PetAttributeValue(BaseModel):
    """
    PetAttributeValue
    """
    # data type: Color
    oneof_schema_1_validator: Optional[Color] = None
    # data type: WeightKg
    oneof_schema_2_validator: Optional[WeightKg] = None
    # data type: Vaccinated
    oneof_schema_3_validator: Optional[Vaccinated] = None
    actual_instance: Optional[Union[Color, Vaccinated, WeightKg]] = None
    one_of_schemas: Set[str] = { "Color", "Vaccinated", "WeightKg" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = PetAttributeValue.model_construct()
        error_messages = []
        match = 0
        # validate data type: Color
        if not isinstance(v, Color):
            error_messages.append(f"Error! Input type `{type(v)}` is not `Color`")
        else:
            match += 1
        # validate data type: WeightKg
        if not isinstance(v, WeightKg):
            error_messages.append(f"Error! Input type `{type(v)}` is not `WeightKg`")
        else:
            match += 1
        # validate data type: Vaccinated
        if not isinstance(v, Vaccinated):
            error_messages.append(f"Error! Input type `{type(v)}` is not `Vaccinated`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in PetAttributeValue with oneOf schemas: Color, Vaccinated, WeightKg. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in PetAttributeValue with oneOf schemas: Color, Vaccinated, WeightKg. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into Color
        try:
            instance.actual_instance = Color.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into WeightKg
        try:
            instance.actual_instance = WeightKg.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into Vaccinated
        try:
            instance.actual_instance = Vaccinated.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into PetAttributeValue with oneOf schemas: Color, Vaccinated, WeightKg. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into PetAttributeValue with oneOf schemas: Color, Vaccinated, WeightKg. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], Color, Vaccinated, WeightKg]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())



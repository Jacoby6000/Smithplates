// Generated by smithplates HTTP codegen. Do not edit.

import { dumpApiModel } from "generated/petstore/api/modelValidation";
import { parseClientResponse } from "generated/petstore/api/client/clientResponse";
import { OPERATION_HTTP_BINDINGS } from "generated/petstore/api/client/operationBindings";
import type { CreatePetInput } from "generated/petstore/api/createPetInput";
import type { CreatePetOutput } from "generated/petstore/api/createPetOutput";
import type { GetPetOutput } from "generated/petstore/api/getPetOutput";
import type { PetAttribute } from "generated/petstore/api/petAttribute";
import type { PetLocationRedirect } from "generated/petstore/api/petLocationRedirect";
import type { PetNotFound } from "generated/petstore/api/petNotFound";
import type { PetSpecies } from "generated/petstore/api/petSpecies";
import type { PetStatus } from "generated/petstore/api/petStatus";
import type { UpdatePetBody } from "generated/petstore/api/updatePetBody";
import type { UpdatePetOutput } from "generated/petstore/api/updatePetOutput";
import type { ValidationError } from "generated/petstore/api/validationError";

export class PetsApiClient {
  private readonly baseUrl: string;
  private readonly fetchFn: typeof fetch;

  constructor(baseUrl: string, fetchFn: typeof fetch = fetch) {
    this.baseUrl = baseUrl.replace(/\/+$/, "");
    this.fetchFn = fetchFn;
  }

  async createPet(
    createPetInput: CreatePetInput,
  ): Promise<CreatePetOutput | ValidationError> {
    const headers: Record<string, string> = {};
    const requestBody = JSON.stringify(dumpApiModel(createPetInput));
    const response = await this.fetchFn(
      `${this.baseUrl}/pets`,
      {
        method: "POST",
        headers,
        body: requestBody,
      },
    );
    const normalized = {
      status: response.status,
      headers: Object.fromEntries(response.headers.entries()),
      text: () => response.text(),
      json: async () => await response.json(),
    };
    const parsed = await parseClientResponse(normalized, OPERATION_HTTP_BINDINGS["createPet"]);
    return parsed as CreatePetOutput | ValidationError;
  }

  async deletePet(
    petId: string,
  ): Promise<PetNotFound | void> {
    const headers: Record<string, string> = {};
    const response = await this.fetchFn(
      `${this.baseUrl}/pets/${petId}`,
      {
        method: "DELETE",
        headers,
      },
    );
    const normalized = {
      status: response.status,
      headers: Object.fromEntries(response.headers.entries()),
      text: () => response.text(),
      json: async () => await response.json(),
    };
    const parsed = await parseClientResponse(normalized, OPERATION_HTTP_BINDINGS["deletePet"]);
    return parsed as PetNotFound | void;
  }

  async getPet(
    petId: string,
  ): Promise<GetPetOutput | PetNotFound> {
    const headers: Record<string, string> = {};
    const response = await this.fetchFn(
      `${this.baseUrl}/pets/${petId}`,
      {
        method: "GET",
        headers,
      },
    );
    const normalized = {
      status: response.status,
      headers: Object.fromEntries(response.headers.entries()),
      text: () => response.text(),
      json: async () => await response.json(),
    };
    const parsed = await parseClientResponse(normalized, OPERATION_HTTP_BINDINGS["getPet"]);
    return parsed as GetPetOutput | PetNotFound;
  }

  async resolvePetLocation(
    petId: string,
  ): Promise<PetLocationRedirect | PetNotFound> {
    const headers: Record<string, string> = {};
    const response = await this.fetchFn(
      `${this.baseUrl}/pets/${petId}/location`,
      {
        method: "GET",
        headers,
      },
    );
    const normalized = {
      status: response.status,
      headers: Object.fromEntries(response.headers.entries()),
      text: () => response.text(),
      json: async () => await response.json(),
    };
    const parsed = await parseClientResponse(normalized, OPERATION_HTTP_BINDINGS["resolvePetLocation"]);
    return parsed as PetLocationRedirect | PetNotFound;
  }

  async updatePet(
    petId: string,
    body: UpdatePetBody,
  ): Promise<UpdatePetOutput | PetNotFound | ValidationError> {
    const headers: Record<string, string> = {};
    const requestBody = JSON.stringify(dumpApiModel(body));
    const response = await this.fetchFn(
      `${this.baseUrl}/pets/${petId}`,
      {
        method: "PUT",
        headers,
        body: requestBody,
      },
    );
    const normalized = {
      status: response.status,
      headers: Object.fromEntries(response.headers.entries()),
      text: () => response.text(),
      json: async () => await response.json(),
    };
    const parsed = await parseClientResponse(normalized, OPERATION_HTTP_BINDINGS["updatePet"]);
    return parsed as UpdatePetOutput | PetNotFound | ValidationError;
  }
}
